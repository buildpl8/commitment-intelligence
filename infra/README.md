# Infrastructure

Infrastructure support files live here when they are not part of the root Docker Compose or Render blueprint.

Current root-level infrastructure files:

- `docker-compose.yml`
- `render.yaml`
- `.env.example`
